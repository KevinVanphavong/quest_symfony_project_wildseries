{% extends 'base.html.twig' %}

{% block title %}Edit Episode{% endblock %}

{% block body %}
    <div class="container-fluid">
        <h1>Edit Episode</h1>

        <div class="my-2">
            {{ include('episode/_form_comment.html.twig', {'button_label': 'Update'}) }}
        </div>

        <div class="my-2">
            {{ include('episode/_delete_form.html.twig') }}
        </div>

        <a href="{{ path('episode_index') }}">back to list</a>
    </div>
{% endblock %}
