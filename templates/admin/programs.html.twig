{% extends 'base.html.twig' %}
{%  block body %}
    <h1>Index des séries</h1>
<section class="container">
    <table class="table">
        <thead>
            <tr>
                <th>Image</th>
                <th>Titre</th>
                <th>Category</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for program in programs %}
            <tr>
                <td><img src="{{ program.poster }}" alt="" style="width:100px"></td>
                <td class="text-light">{{ program.title }}</td>
                <td>{{ program.category.name }}</td>
                <td>
                    <div class="d-flex">
                        <a class="btn btn-outline-secondary mx-2" href="{{ path('program_edit', {id: program.id}) }}">Modifier</a>
                        <p class="mx-2">{{ include('program/_delete_form.html.twig') }}</p>
                    </div>
                </td>
            </tr>
    {% endfor %}
        </tbody>
    </table>
    <div class="text-right">
        <a class="btn btn-outline-primary m-5" href="{{ path('program_new') }}">Ajouter</a>
    </div>
</section>
{% endblock %}
