{% extends 'base.html.twig' %}
{%  block body %}
    <h1>{{ 'Répertoire des acteurs'|trans }}</h1>
    <section class="container">
        <table class="table">
            <thead>
            <tr>
                <th>{{ 'Nom'|trans }}</th>
                <th>Actions</th>
            </tr>
            </thead>
            <tbody>
            {% for actor in actors %}
                <tr>
                    <td>{{ actor.name }}</td>
                    <td>
                        <div class="d-flex">
                            <a class="btn btn-outline-secondary mx-2" href="{{ path('actor_edit', { id: actor.id }) }}">{{ 'Modifier'|trans }}</a>
                            <p class="mx-2">{{ include('actor/_delete_form.html.twig') }}</p>
                        </div>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
        <div class="text-right">
            <a class="btn btn-outline-primary mx-2" href="{{ path('actor_new') }}">{{ 'Ajouter'|trans }}</a>
        </div>
    </section>


{% endblock %}
