{% extends 'base.html.twig' %}
{%  block body %}
    <h1>Index des catégories</h1>

    <table class="table container">
        <thead>
        <tr>
            <th>Categorie</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for category in categories %}
            <tr>
                <td>{{ category.name }}</td>
                <td>
                    <div class="d-flex">
                        <a class="btn btn-outline-primary mx-2" href="{{ path('episode_new') }}">Ajouter</a>
                        <a class="btn btn-outline-secondary mx-2" href="{{ path('episode_edit', { id: episode.id }) }}">Modifier</a>
                        <p class="mx-2">{{ include('episode/_delete_form.html.twig') }}</p>
                    </div>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
