{% extends 'base.html.twig' %}

{% block title %}Edit Episode{% endblock %}

{% block body %}
    <div class="container-fluid">
        <h1>Modifier série " {{ program.title }} "</h1>

        <div class="my-2">
            {{ include('program/_form.html.twig', {'button_label': 'Update'}) }}
        </div>

        <div class="my-2">
            {{ include('program/_delete_form.html.twig') }}
        </div>

        <a href="{{ path('program_index') }}">Retour</a>
    </div>
{% endblock %}
