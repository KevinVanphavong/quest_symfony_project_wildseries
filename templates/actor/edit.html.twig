{% extends 'base.html.twig' %}

{% block title %}Edit Episode{% endblock %}

{% block body %}
    <div class="container-fluid">
        <h1>Edit Episode</h1>

        <div class="my-2">
            {{ include('actor/_form.html.twig', {'button_label': 'Modifier'}) }}
        </div>

        <div class="my-2">
            {{ include('actor/_delete_form.html.twig') }}
        </div>

        <a href="{{ path('admin_actors') }}">Retour</a>
    </div>
{% endblock %}
